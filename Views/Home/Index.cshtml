@{
    ViewData["Title"] = "Home Page";
    // In a real app, you would pass a list of Product models from your controller
}

<!-- ======================= HERO SECTION ======================= -->
<section class="hero" style="background-image: url('/images/hero-bg.jpg');">
    <div class="hero-content">
        <h1>Illuminate Your Passion</h1>
        <p>Curated merch that brings your favorite universes to light.</p>
        <a href="#" class="btn btn-primary">Shop New Arrivals</a>
    </div>
</section>

<!-- ======================= BESTSELLERS ======================= -->
<section class="section container">
    <h2>Fan Favorites: Our Bestsellers</h2>
    <div class="product-grid">
        @* In a real application loop through your product models here.
            For example:
            @foreach (var product in Model.Bestsellers)
            {
                <partial name="_ProductCard" model="product" />
            }
            For now, we will just render the partial with dummy data.
        *@
        
        <partial name="_ProductCard" />
        <partial name="_ProductCard" />
        <partial name="_ProductCard" />
        <partial name="_ProductCard" />
    </div>
</section>